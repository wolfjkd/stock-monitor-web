# 2026-06-08 工作日志

## stock-monitor-web v3.0 开发

### 完成内容
- 从GitHub克隆stock-monitor-web仓库到本地
- 实现Flask后端API（app.py）：
  - `/api/quotes` - 实时行情API
  - `/api/config` - 监控配置GET/POST
  - `/api/monitor/start|stop|status` - 监控控制
  - `/api/alerts/stream` - SSE预警日志流
  - `/api/alerts/clear` - 清空预警日志
  - `/api/nodes` - TDX节点列表
  - `/api/nodes/switch` - 切换节点
- 实现Bootstrap 5前端页面（templates/index.html）：
  - 实时行情卡片展示（6只自选股）
  - 监控配置表格（在线编辑/添加/删除）
  - 预警日志区域（SSE实时滚动）
  - 监控启停控制
  - 节点选择下拉菜单
- 实现JavaScript交互逻辑（static/js/main.js）
- 创建requirements.txt、run.bat、run.sh
- 安装依赖：Flask 3.1.3 + eltdx 1.0.2
- 测试通过：所有API正常，实时行情获取成功（TDX郑州节点）
- 代码已提交并推送到GitHub

### 多节点支持（新增）
- 新增 tdx_nodes.json 节点配置
- 3个TDX节点（老板优选低延迟）：
  - 云行情郑州主站Z1: 182.118.8.4:7709
  - 云行情上海主站Z1: 114.141.177.44:7709
  - 云行情北京主站Z1: 123.125.108.103:7709
- 支持故障转移（按优先级自动切换）
- 前端支持手动切换节点

### 项目结构
```
stock-monitor-web/
├── app.py              # Flask后端
├── templates/          # HTML模板
├── static/js/          # JavaScript
├── scripts/            # CLI版核心代码（复用）
│   ├── price_alert.py  # 核心监控脚本（含多节点支持）
│   ├── tdx_nodes.json  # TDX节点配置
│   └── watchlist_config.json
├── requirements.txt    # 依赖
├── run.bat/run.sh      # 启动脚本
└── .gitignore
```

### 运行方式
```bash
cd stock-monitor-web
python app.py
# 浏览器访问 http://localhost:5000
```
